rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Users — authenticated users can read/write their own profile
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // Feeds — anyone can read; only admins can write
    match /feeds/{feedId} {
      allow read: if true;
      allow write: if request.auth != null
        && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == "admin";
    }

    // Sectors — anyone can read; only admins can write
    match /sectors/{sectorId} {
      allow read: if true;
      allow write: if request.auth != null
        && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == "admin";
    }

    // Milestones — anyone can read; only admins can write
    match /milestones/{milestoneId} {
      allow read: if true;
      allow write: if request.auth != null
        && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == "admin";
    }

    // Notifications — anyone can read; admins can create; authenticated users can update/delete their own
    match /notifications/{notifId} {
      allow read: if true;
      allow create: if request.auth != null
        && get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == "admin";
      allow update, delete: if request.auth != null;
    }

    // Messages — anyone can read; authenticated users can write
    match /messages/{messageId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // Presence — authenticated users can write their own; anyone can read
    match /presence/{userId} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
